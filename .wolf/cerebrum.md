# Cerebrum

> OpenWolf's learning memory. Updated automatically as the AI learns from interactions.
> Do not edit manually unless correcting an error.
> Last updated: 2026-05-12

## User Preferences

<!-- How the user likes things done. Code style, tools, patterns, communication. -->

## Key Learnings — PWA

- **Phase 4 PWA Polish (2026-05-17):** Safe-area insets require `viewport-fit=cover` in viewport meta — without it, `env(safe-area-inset-*)` always resolves to 0. Topbar uses `min-height + padding-top: env(safe-area-inset-top)` instead of `height` to accommodate notch on iPhone standalone. `.view-panel` uses `padding-bottom: calc(32px + env(safe-area-inset-bottom, 0px))`. Mobile fixed sidebar gets `padding-top: env(safe-area-inset-top)` inside the `@media (max-width: 900px)` block.
- **Dark flash prevention pattern:** Add inline `<script>` in `<head>` (before any CSS link) that reads `dandyDarkMode` from localStorage and sets `document.documentElement.style.background = '#0B2B1F'` + `colorScheme = 'dark'`. This sets the html-level bg before body renders. Body's `transition: background .4s` smooths the transition when `body.dark` is applied later by `applyDarkMode()`. No CSS selectors needed.
- **PWA manifest: `background_color` must match `--c-brand-bg` token (`#EDF6F1`):** Was `#EEF2EF` (off). Correct value is `#EDF6F1`. Add `scope: './'`, `lang: 'en'`, `id: './'`, `prefer_related_applications: false`. Icon needs two entries: `purpose: 'any'` and `purpose: 'maskable'` (maskable icon centers content in safe zone — no rounded corners, no transparency).
- **`* { -webkit-tap-highlight-color: transparent; }` is safe for clinical PWAs:** Removes grey iOS tap flash without affecting keyboard focus states or `focus-visible` outlines. Add at top of CSS block.
- **`height: 100dvh`** (dynamic viewport height) prevents layout shift on mobile when address bar appears/disappears. Use `height: 100vh; height: 100dvh;` — browsers that don't support `dvh` fall back to `vh`. `100dvh` = exact visible viewport height.
- **`.page overscroll-behavior-y: contain`** prevents the iOS rubber-band bounce from propagating from the `.page` scroll container to the whole window — keeps scroll within the panel. Pair with `overscroll-behavior: none` on body.

## Key Learnings

- **Project:** denai — single-file clinical app (`index.html`, ~102k tok). All CSS is inline `<style>`. Token extraction pattern: create `src/styles/tokens/<group>-tokens.css`, add `<link>` before `<style>`, remove declarations from `:root` and `body.dark`.
- **Token extraction governance:** Light-mode and dark-mode overrides for a token family must be co-extracted as a single atomic unit. Never extract light-mode tokens without their `body.dark` counterparts.
- **Coupling pattern:** `--surface-sidebar: var(--c-brand-dark)` is a direct token reference. Renaming `--c-brand-dark` would silently break sidebar background. Treat as a known one-way dependency.
- **RGB tokens added (Phase 1 Final Cleanup, 2026-05-17):** `--c-brand-rgb: 31, 122, 79` and `--c-brand-rgb-2: 42, 156, 103` added to `brand-tokens.css` `:root`. All hardcoded `rgba(31,122,79,...)` and `rgba(42,156,103,...)` across `index.html`, `cards.css`, `shadow-tokens.css`, `focus-tokens.css`, `costGraphPanel.js` replaced with `rgba(var(--c-brand-rgb),alpha)` / `rgba(var(--c-brand-rgb-2),alpha)`. Token values use COMMA-separated channels (e.g. `31, 122, 79`) so the `rgba(var(...),.alpha)` legacy-comma CSS syntax works — space-separated would require the slash syntax `rgba(var(...) / alpha)` instead.
- **`'unsafe-eval'` was unnecessary and has been removed (Wave 6C.1A):** Full grep of all JS files and inline script confirmed zero `eval()`, `new Function()`, or string-based `setTimeout`/`setInterval`. Do not re-add without a confirmed runtime requirement.
- **`blob:` URLs used in 4 features — CSP gap closed (2026-05-16):** (1) report popup `window.open(blobUrl)`, (2) JSON export download link, (3) PWA manifest built dynamically in JS, (4) SW registration. Fixed by: `default-src 'self' blob:` (covers popup/download/manifest) + new `worker-src 'self' blob:` directive (covers SW — required because `worker-src` falls back to `script-src`, not `default-src`, and `script-src` was set without blob:).
- **Service Worker cache name fixed to `denai-v2.0` (2026-05-16):** SW code at `index.html` ~line 5245 previously hardcoded `const CACHE = 'dandy-v1'` (old project name). Fixed to `'denai-v2.0'` in beta-hardening fix #1. Any future SW version bump should use `denai-v2.x`.

- **Wave 7C cloud schema: `state` JSONB excludes `notes` and `activeSite`:** The Wave 7D serializer must strip both before upload. `notes` = PHI (deferred to Wave 7G encryption). `activeSite` = device-local navigation state (meaningless on another device). Omitting from JSONB causes `{ ...DEFAULT_STATE, ...stateFromCloud }` to restore `activeSite = 1` correctly.
- **Patient IDs are the cloud PK — format is frozen:** `p_<timestamp>_<random>` maps to `text PRIMARY KEY` in the `patients` table. Changing this format after Wave 7D would break all existing cloud rows.
- **History is embedded in the `patients.history` JSONB column (not a separate table):** Capped at 50 entries, append-only, per-patient. Separate table adds complexity for no gain at current scale.
- **`notes_enc` column exists but is NULL until Wave 7G:** The schema reserves the column. Do not populate it before client-side AES-GCM encryption is implemented. Key management design must precede Wave 7G.

- **Wave 7B auth initializes AFTER render(S) via `denaiAuth.init()` at end of `init()`:** Auth is purely additive. The call is `denaiAuth.init().catch(() => {})` — never throws. All auth failures silently degrade to local mode. localStorage clinical data is NEVER touched by auth operations.
- **Supabase CDN global is `window.supabase` (not `window.Supabase`):** The UMD bundle from `cdn.jsdelivr.net/npm/@supabase/supabase-js@2.x/dist/umd/supabase.js` exposes `window.supabase = { createClient: ... }`. Reference it as `window.supabase.createClient(url, key)`.
- **Auth placeholder credentials cause graceful local-mode fallback:** `placeholder.supabase.co` will fail client init. `_getClient()` returns `null`, `_restoreSession()` catches and calls `_setStatus('local')`. App continues normally. Replace with real credentials to activate cloud.
- **Versioned asset count is now 30 (not 29):** Wave 7B added `src/auth/authModule.js?v=2.0.0`. Release checklist grep count updated to 30.

- **Wave 7D sync queue uses explicit upsert deduplication:** When a new upsert for the same `patient_id` arrives, the old queued upsert is replaced (filter+push). The newest payload is always correct; stale queued upserts are wasteful.
- **Wave 7D soft-delete enqueue must happen BEFORE localStorage filter:** `enqueueSoftDelete(_pendingDeleteId)` is called in `confirmDeletePatient()` before `list.filter(...)` — the patient ID must still exist in-scope when the op is enqueued.
- **Wave 7D flush triggers: both `_restoreSession` AND `_listenAuthChanges` need flush calls:** `getSession()` does not fire `onAuthStateChange` (SIGNED_IN event) — so a returning user whose session is restored via `getSession()` would never flush the queue without the explicit `setTimeout(() => denaiSyncQueue.flush(), 0)` in `_restoreSession`.
- **Wave 7D sync status display shares `#authUserPlan` with authModule:** `syncQueue._setStatus()` guards with `denaiAuth.isSignedIn()` before writing. authModule owns the element in local mode; syncQueue owns it only when signed in. Occasional text flip during token refresh is cosmetic only.
- **Wave 7D `denaiAuth.getClient()` is the single Supabase client for DB ops:** syncQueue uses `denaiAuth.getClient()` rather than creating a second client. Two clients with the same credentials would share auth session but could cause double token-refresh races.
- **Wave 7D versioned asset count is now 32 (not 30):** 19 CSS + 13 JS (serializer.js + syncQueue.js added). Release-checklist grep count updated to 32. Deployment-validation total requests updated to 33.
- **Wave 7D history is included in upsert payload but synced opportunistically:** `History.load(S.id)` is called inside saveState's debounce. History changes alone (no state change) do not trigger a cloud sync in 7D. Wave 7E concern.
- **Wave 7D upsert `updated_at` is overridden by server trigger on UPDATE:** The `touch_updated_at()` trigger sets `updated_at = now()` on the server for any UPDATE (including upsert conflict resolution). Client-provided `updated_at` is used for INSERT only. This makes last-write-wins conflict detection unavailable for Wave 7D — deferred to Wave 7E.

- **Wave 7E `_syncedAt` is local-only metadata on each patient object:** It holds the cloud `updated_at` ISO string from the last time that patient was received from Supabase. It is NOT in `ALLOWED_FIELDS` (serializer strips it). It persists in localStorage alongside the patient and is used by `cloudSync._mergeOne()` as the merge baseline. Never treat it as clinical data.
- **Wave 7E `hasPendingFor(patientId)` guards against cloud overwriting in-flight local edits:** If a patient has an unsent upsert in the queue (edited locally but not yet flushed), the merge engine keeps local unchanged even if cloud claims to be newer. This protects the "offline edit then come online" scenario.
- **Wave 7E `denaiApplyCloudMerge` MUST be a function declaration (not const) in the inline script:** cloudSync.js is a separate classic-script file. It can only access top-level function declarations from the inline script via window.* — `const`/`let` top-level vars are not on window. `denaiApplyCloudMerge` needs access to `S`, `UIState`, `DEFAULT_STATE` — all `const` in inline scope — so it must live IN the inline script and be exposed as a function declaration.
- **Wave 7E `_isPlaceholder()` prevents uploading the default 'Mohamed A.' seed patient to cloud:** A patient is a placeholder if: name === 'Mohamed A.' AND no `_syncedAt` (never came from cloud) AND no history entries. This avoids polluting new device accounts with a meaningless default record.
- **Wave 7E merge passes history only when cloud wins:** `_saveHistory()` is called only when `merged !== local` (cloud version replaced local). If local wins (unchanged), local history is preserved and NOT overwritten by possibly-older cloud history.
- **Wave 7E versioned asset count is now 33 (not 32):** 19 CSS + 14 JS (cloudSync.js added). Release-checklist grep count updated to 33. Deployment-validation total requests updated to 34.
- **Wave 7E `render` and `buildEditForm` ARE accessible from external script files:** They are function declarations at the top level of the inline `<script>` block. The 2-space indentation is stylistic only — JavaScript scope is not affected by indentation. `const`/`let` at the same indentation level are still NOT on window.

- **Wave 7F `let darkMode` is NOT on window — use `window.denaiSetDarkMode` bridge:** `let darkMode` in the inline script is inaccessible from `prefsSync.js`. The bridge function `function denaiSetDarkMode(value)` (function declaration = IS on window) sets `darkMode`, updates `dandyDarkMode` localStorage key, and calls `applyDarkMode()`. `denaiPrefs._applyToDom()` calls `window.denaiSetDarkMode`.
- **Wave 7F preferences key is `denaiPrefs_v1`, falls back to `dandyDarkMode` for backward compat:** On first load, if `denaiPrefs_v1` is absent, `prefsSync.js` reads `dandyDarkMode`. After first `save()`, `denaiPrefs_v1` is written and becomes the canonical source.
- **Wave 7F tombstone cleanup uses a targeted second query (not full deleted rows fetch):** `_fetchTombstones(client, ids)` queries only `patients` rows matching specific local patient IDs with `deleted_at IS NOT NULL`. This avoids fetching all deleted rows on every hydrate. The IDs passed are local patients absent from the non-deleted cloud result set.
- **Wave 7F tombstone guard: `hasPendingFor` supersedes cloud delete:** If Device A has an unsent upsert for a patient that Device B deleted, Device A keeps the patient locally. The pending upsert will re-create it in cloud. Same guard as merge engine.
- **Tombstone Pass 3 is blocked by Pass 2 in normal flow (known interaction, 2026-05-16):** `_mergeCloudIntoLocal` runs Pass 2 (enqueue local-only patients) BEFORE Pass 3 (tombstone removal). Pass 2 calls `enqueue()` for any local patient absent from cloudById — including tombstoned patients. This sets `hasPendingFor=true`, which Pass 3's guard uses to abort removal. Net effect: "local data wins" over cloud delete. To test Pass 3 in isolation, use `makeNoPendingQueueStub()` (see `tests/sync/runner.js`) which makes `hasPendingFor` always return false and `enqueue` a no-op.
- **Wave 7F `denaiApplyCloudMerge` now handles tombstoned active patient:** If `changedIds` contains `S.id` but the patient is no longer in `loadAllPatients()`, the patient was deleted on another device. `switchPatient(fallback.id)` is called. Previously the function returned early and left a stale UI.
- **Wave 7F versioned asset count is now 34 (not 33):** 19 CSS + 15 JS (prefsSync.js added). Release-checklist grep count updated to 34. Deployment-validation total requests updated to 35.
- **Wave 7F `exportAllData()` / `importAllData()` use `exportFormat: 'denai-backup-v1'` sentinel:** Import validation rejects any file without this exact sentinel. Prevents importing unrelated JSON as patient data. Import never overwrites existing patient IDs (merge-safe).
- **Wave 7F `getLastSyncedAt()` (syncQueue) and `getLastHydratedAt()` (cloudSync) expose sync timestamps:** Both return ISO strings or null. `openAuthModal()` calls `_newerTs()` to show the most recent of upload/download timestamps in the account panel.
- **Wave 7F profiles table upsert uses `onConflict: 'user_id'`:** The Supabase `profiles` table has `user_id uuid PRIMARY KEY`. Upsert with `{ onConflict: 'user_id' }` is idempotent — safe to call on every preference change.

- **Wave 7G encryption architecture — AES-GCM 256-bit, PBKDF2 passphrase-derived key:** Key = PBKDF2(passphrase, salt, 100000 iterations, SHA-256) → AES-GCM 256-bit. Salt (non-sensitive) stored in `profiles.preferences.notesKeySalt`. Passphrase lives in memory only (CryptoKey closure). Key cleared on sign-out. Multi-device: same passphrase + same salt → identical key on any device.
- **Wave 7G `notes_enc` is a SEPARATE top-level column, NOT inside `state` JSONB:** The serializer output goes into `state`. `notes_enc` is added as a top-level key on the upsert row directly in `syncQueue._executeOp`. cloudSync SELECTs `notes_enc` separately. Never nest `notes_enc` inside the serializer's output.
- **Wave 7G `rawNotes` stored at enqueue, encrypted at flush time:** The serializer strips `notes` at enqueue. `rawNotes` is captured BEFORE serialization and stored on the queue item. At flush (already async), `denaiNotesEnc.encrypt(op.rawNotes)` is called if key is available. This handles the case where the user enters their passphrase AFTER the save but BEFORE the flush.
- **Wave 7G `notes_enc` never sent as null to overwrite existing ciphertext:** If no key available, `notes_enc` is omitted from the upsert row entirely. Supabase upsert without the column leaves the existing value unchanged. Sending `null` would destroy ciphertext stored by another device.
- **Wave 7G decryption happens BEFORE merge in cloudSync:** `_fetchAndMerge` runs an async decrypt pass on all cloud rows → builds `decryptedNotesMap: { id → plaintext }`. This map is threaded through `_mergeCloudIntoLocal → _mergeOne → _buildMerged`. Keeps merge logic synchronous.
- **Wave 7G `_triggerPassphrasePrompt()` in prefsSync calls `window.denaiShowNotesPassphrasePrompt`:** Same bridge pattern as `denaiSetDarkMode`. Function declaration in inline script = accessible as `window.*`. Called at the end of every `hydrate()` attempt (success or failure). Guards: skip if no salt, skip if key already derived (`denaiNotesEnc.hasKey()`).
- **Wave 7G passphrase prompt is per-session, skippable, non-blocking:** `_notesPromptShown` var in inline script prevents re-showing. "Skip" = no cloud notes for this session; local notes always accessible. `denaiResetNotesPrompt()` called on sign-out so prompt re-appears on next sign-in.
- **Wave 7G `setupNotesEncryption()` generates salt and saves to prefsSync BEFORE showing prompt:** `denaiNotesEnc.generateSalt()` → `denaiPrefs.save({ notesKeySalt: salt })` → shows modal in setup mode. Salt is saved to localStorage + scheduled for cloud push immediately, so it persists even if user refreshes before completing the passphrase prompt.
- **Wave 7G versioned asset count is now 35 (not 34):** 19 CSS + 16 JS (notesEncryption.js added). Release-checklist grep count updated to 35. Deployment-validation total requests updated to 36.
- **Wave 7G local notes remain plaintext in localStorage — local-first invariant preserved:** Only cloud sync gets encrypted notes. `S.notes` and localStorage are never touched by encryption. Decrypt failure → local notes survive unchanged. Offline mode fully functional with no crypto dependency.

- **Wave 8A/8B nav architecture:** Primary nav is 6 items: Dashboard, Cases, AI Insight, Treatment Plan, Lab & Mfg, Reports. Orders and Messages are cut — they create noise in a clinical workflow tool. Nav items use `onclick="navigateTo('view')"` + `data-view` attr. `navigateTo()` is a top-level function declaration (window-accessible). Sidebar max-height removed.
- **Wave 8B view-panel system:** `.view-panel` siblings inside `.page`. Only one has `.view-active + .view-visible`. `.page` padding was moved to individual panels (padding: 22px 26px 32px on each). `navigateTo()` swaps classes, scrolls `.page` to top, updates topbar title/icon, updates nav active state, re-renders view-specific content.
- **Wave 8B workflow bar:** `#workflowBar` is populated by `renderWorkflowBar()`. Derives stage from `S.condition`→scan, `S.planApproved`→plan, `S.labStatus`→lab. For Wave 8B: scan=done, insight=active, plan/lab=pending (since planApproved/labStatus not in S yet). No hardcoding.
- **Wave 8B entry overlay:** `#entryOverlay` (position:fixed, z-index:300) shows on first visit if `denaiEntryDismissed_v1` localStorage key is absent. `continueLocally()` sets the flag + fades overlay out. "Sign In" button calls both `openAuthModal()` and `continueLocally()` so overlay dismisses when auth modal opens.
- **Wave 8B topbar patient subtitle:** `renderMainPanels()` updates `#topbarPatientName` with `state.name + age` after each render. Previously hardcoded "Live Analysis Active".
- **Wave 8A frozen vs. active zones:** `render()`, `ClinicalEngine`, `S` field schema, auth module, and sync pipeline are all frozen. Only additive changes to UIState and new view panel DOM are safe.

- **Wave 8C: workflow continuity fields.** `planApproved: false`, `labStatus: null`, `lastAccessed: null`, `reportHistory: []` added to `DEFAULT_STATE`. Migration-safe: existing patients get defaults via `{ ...DEFAULT_STATE, ...loaded }`. `planApproved` + `labStatus` ARE in serializer `ALLOWED_FIELDS` (they follow the patient across devices). `lastAccessed` + `reportHistory` intentionally LOCAL-ONLY (device-local timestamp, large array).
- **Wave 8C: workflow action buttons.** `approvePlan()`, `sendToLab()`, `reopenPlanning()` directly mutate `S.planApproved` / `S.labStatus` then call `saveState()` + `renderWorkflowBar()` (which re-renders actions too). They BYPASS `setState()` intentionally — these fields have no schema in `StateValidator.SCHEMAS`, so `setState()` would pass them through, but direct mutation is cleaner for boolean/null fields outside the validation domain.
- **Wave 8C: `renderWorkflowActions()` called at end of `renderWorkflowBar()`.** Single trigger point ensures bar and actions always stay in sync. Also called directly after approvePlan/sendToLab/reopenPlanning.
- **Wave 8C: `reportHistory` is mutated directly on `S`, not via `setState()`.** Arrays don't fit the string/number/enum validation schema. Mutate `S.reportHistory` directly, then call `saveState()`. Cap at 20 entries with `.slice(0, 20)`. `reportHistory` accumulates in localStorage only; serializer strips it.
- **Wave 8C: workspace restoration uses `sessionStorage`.** `sessionStorage` is per-tab — refresh restores the view, closing the tab resets to insight. This is the correct UX: intentional navigation restored, tab closure = fresh start. Key: `denaiLastView_v1`.
- **A1 (Phase 1A Session 3): `lastView: null` in DEFAULT_STATE — LOCAL ONLY, NOT in serializer ALLOWED_FIELDS.** Tracks last navigated patient-scoped view ('insight'|'plan'|'lab'|'reports'). Set via `S.lastView = view; saveState()` inside `navigateTo()` before the view-specific render calls (step 6). Guards to patient-scoped views only: `['insight','plan','lab','reports'].includes(view)`. `openCase(id)` reads `S.lastView` after `switchPatient(id)` (which loads target patient's state synchronously via Object.assign) and navigates there; falls back to stage-derived destination: `labStatus==='received'`→reports, `planApproved && labStatus`→lab, `planApproved && !labStatus`→plan, default→insight. `saveState()` is debounced 200ms — snapshot taken at call time. `switchPatient`'s 300ms setTimeout also calls `saveState()` (for lastAccessed) — both writes land cleanly, no race.
- **Wave 8C: `lastAccessed` stamped in `switchPatient()` setTimeout callback and in `init()`.** Called after `render(S)` so the timestamp doesn't delay the first paint. `saveState()` call in the same callback persists it to the patient list immediately.
- **Wave 8C: `_getPatientStageBadge(p)` and `_relativeTime(ts)` work on raw patient objects (not `S`).** They are used in case cards for ALL patients, not just the active one. Never call them with `S` if you mean "a patient from the list" — the patient list object may differ from in-memory `S` until `saveState()` flushes.

- **Wave 8D motion system:** `--t-base` unified to `cubic-bezier(.16,1,.3,1)` (was Material easing `.4,0,.2,1`). Added `--t-slow: .4s` for theme/background transitions. `--t-fast` also uses spring curve now. All interactive durations ≤250ms. `--transition-card` no longer includes `opacity` (cards don't fade on hover).
- **Wave 8D: never use will-change on all cards.** `will-change: transform` in the reduced-motion block was promoting EVERY card to a GPU compositing layer — expensive with many cards. Remove it; the browser handles the modest 2px lift without hints.
- **Wave 8D: nav item hover should NOT translateX.** The horizontal slide on sidebar nav items is distracting. Sidebar nav hover = background/color only. Reserve translateX for drawer-style components, not fixed-position nav items.
- **Wave 8D: ECG heartbeat + reduced-motion.** Animation `ecgDraw` had no `prefers-reduced-motion` override. Always add one. Also: continuous looping animations in clinical UI add visual fatigue. Use animation-delay to create a pause between cycles.
- **Wave 8D: `comp-cell.dimmed` opacity.** Default was `.28` (too harsh), already overridden in Polish section to `.70`. Confirm this override survives future edits.
- **Wave 8D: case-time should be dynamic.** Hardcoded "Last updated: Today, 10:42 AM" in the case header creates clinical confusion. `renderMainPanels()` now updates `.case-time` using `_relativeTime(state.lastAccessed)`.

- **Wave 8E: Plan and Lab views derive all content from `_getAiForPlan(S)`.** This private helper safely calls `ClinicalEngine.process(S)` or `processCompound(S)` in a try/catch. Never call ClinicalEngine directly inside a view renderer without a safety wrapper — engine throws if state is malformed.
- **Wave 8E: Lab view gates on `S.planApproved`.** If not approved, shows a "not ready" state with navigation back to Plan. This maintains workflow integrity without any arbitrary UI flags.
- **Wave 8E: `labNotes` is LOCAL-ONLY.** Added to `DEFAULT_STATE` but NOT to serializer `ALLOWED_FIELDS`. Manufacturing notes are device-local workflow annotations, not clinical data. Saved via direct `S.labNotes = el.value; saveState()` in `saveLabNotes()`.
- **Wave 8E: `markLabReceived()` advances labStatus to 'received'.** `sendToLab()` → 'pending', `markLabReceived()` → 'received'. Removed the ambiguous 'sent' intermediate state from the Lab UI (3 steps only: Plan Approved → Sent to Lab → Received).
- **B5 (Phase 1B): Persistent workflow context bar — shared shell pattern.** `#workflowBar` + `#workflowActions` elevated from inside `#viewInsight` to a new `#patientWorkflowShell` div that is a direct child of `.page`, before all `.view-panel` siblings. `navigateTo()` toggles `.wf-shell-visible` on BOTH the shell and `.page`. CSS: `#patientWorkflowShell { padding: 22px 26px 0; display: none; }`, `#patientWorkflowShell.wf-shell-visible { display: block; }`, `.page.wf-shell-visible .view-panel { padding-top: 0; }` (shell provides the top padding, view panels lose their own to avoid double-spacing). `renderWorkflowBar()` needs no changes — it targets `#workflowBar` by ID regardless of DOM parent. Init: after `renderWorkflowBar()`, explicitly add `wf-shell-visible` to both shell and `.page` (insight is the default active view). If workspace restores to non-patient view, `navigateTo()` removes the class. Patient-scoped views: insight, plan, lab, reports. Non-patient views: dashboard, cases.
- **C1 Phase 1 (Phase 1C): Workflow event infrastructure.** `wfHistory: []` in DEFAULT_STATE — local-only, NOT in serializer ALLOWED_FIELDS (same class as `reportHistory`). Three helpers: `_emitWfEvent(type, meta?)` — prepends to S.wfHistory, 5-second duplicate guard by checking `S.wfHistory[0]`, cap 60, calls saveState (debounce-safe since callers also call saveState — trailing-edge debounce captures both mutations); `_synthesizeWfBaseline(p)` — pure function, returns synthetic baseline events (ts:null, synthetic:true) for legacy patients derived from planApproved/labStatus state; `_checkEmitAnalysisCompleted()` — lazy, guarded by `.some(e => e.type === 'analysis_completed')`, wrapped in try/catch for calcAI safety, emits once per patient lifetime. Emission points: 4 action functions (approvePlan/sendToLab/markLabReceived/reopenPlanning) get one-liner after state mutation; openReport() inner function covers all 3 report paths (compound/multi-tooth/single) with one addition; _checkEmitAnalysisCompleted called in navigateTo() step 7 for 'insight' + in init sequence after renderWorkflowBar(). `report_generated` added to openReport inner function — single addition covers all three generateReport() paths.

- **C1 (Phase 1C): Workflow urgency semantics — `_getCaseUrgency(p)` + CSS inset accent.** Three urgency classes: `urg-received` (labStatus==='received'), `urg-waiting` (planApproved && !labStatus), `urg-new` (!p.condition). CSS approach: set `--_urg-c` CSS custom property on the class, then target `.case-card.urg-*` and `.dash-attention-item.urg-*` for `inset 3px 0 0 var(--_urg-c)` box-shadow. Hover combines outer `shadow-md` + inset accent. Applied to: Cases View renderCard(), Dashboard recent-case map, Dashboard attention items (added `urgCls` field to items array). Colors: brand green (received), rgba(37,99,235) blue (waiting), rgba(245,158,11) amber (new). Attention items already had semantic icon colors — urgency adds a left-bar accent for visual consistency across surfaces. No new schema, no persistence change.

- **B5 (Phase 1B): `.wf-pending` color contrast fix.** `--c-n400` (#9CA3AF) on `--surface-page` (#EEF2EF) = 2.14:1 — fails WCAG AA. Axe detected this once the bar left the `.view-panel` opacity/transform stacking context. Fix: `--c-n600` (#4B5563) = 5.92:1. Pending steps are still clearly subdued vs. done (`--c-brand` green) and active (white on brand bg). Dark mode `--c-n600: #bac0cc` on dark page is high-contrast.

- **B3 (Phase 1B): Sidebar scaling — the flex growth fix.** Sidebar is a column-flex container. Root problem: `.sidebar-nav` had `flex: 1` (ate all remaining space); `.patient-section` had `flex-shrink: 0` (fixed natural height); `.patient-list` had `max-height: 190px` (capped at ~5 items). Fix: `.sidebar-nav → flex: 0 0 auto; min-height: 0` (nav collapses to content, keeps overflow-y: auto for safety); `.sidebar-user → flex-shrink: 0` (stays pinned at bottom); `.patient-section → flex: 1; min-height: 0; display: flex; flex-direction: column; overflow: hidden` (becomes the growing zone); `.patient-list → flex: 1; min-height: 0; overflow-y: auto` (no max-height, fills patient-section). The "New Patient" button inside patient-section remains at `flex: 0 0 auto` (default) and stays pinned below the scrollable list. Works identically on mobile (sidebar is `position: fixed; height: 100%`). Key insight: `min-height: 0` on both the flex container and the scrollable child is required — without it, the flex child cannot be smaller than its content and overflow-y: auto never activates.

- **B1 (Phase 1B): Cases view stage filter tabs.** `let _casesStageFilter = 'all'` alongside `_casesFilter`. `setCasesStageFilter(stage)` updates the `active` class + `aria-selected` on `.cases-tab[data-stage]` buttons, then calls `renderCasesView()`. In `renderCasesView()`: text search produces `textFiltered`, stage filter uses a `stageFilter` map object (`{ new, analysis, lab, delivered }`) producing `filtered`. Active patient separation uses the final `filtered` — if active patient doesn't match the stage filter, the "Active Patient" section simply doesn't render. Empty-state message has 3 paths: noTotal (no patients yet, show New Patient btn), hasStage (stage filtered everything, no New Patient btn), else text search miss. Filter tab bar is in static HTML (`role="tablist"`) between `.cases-header` and `#casesViewContent` — not inside the render container, so it survives `renderCasesView()` innerHTML rewrites.

- **B4 (Phase 1B): Pipeline stats strip in `renderDashboardView()`.** Compact horizontal strip with 4 stage counts: New (`!p.condition`), In Analysis (`p.condition && !p.planApproved`), At Lab (`p.planApproved && p.labStatus && p.labStatus !== 'received'`), Delivered (`p.labStatus === 'received'`). Reuses the single `allPatients` load at the top of the function — no extra `loadAllPatients()` call. Uses a scoped `_setPipe(id, count)` helper to set text + `.zero` class (dims to `var(--c-n300)`) when count is 0. CSS: `.dash-pipeline-strip` flex row with `overflow:hidden` + `.dash-pipeline-div` (1px border) for internal dividers — cleaner than border-right on stats (avoids double-border at edges). Stage filter logic here partially overlaps with `_getPatientStageBadge(p)` and the attention items section — future extraction opportunity if stages are added or changed.

- **Wave 8E: Dashboard attention items use `loadAllPatients()` scan.** Scans all patients for `planApproved && !labStatus` (lab-ready) and `labStatus === 'received'` (delivery needed). Cap at 5 items. Hidden when empty via `style.display='none'`. This is a read-only surface — clicking opens the case via `openCase(id)`.
- **Wave 8E: view re-renders triggered from action functions, not render().** `approvePlan()`, `sendToLab()`, `markLabReceived()`, `reopenPlanning()` each check `UIState.view` and call the appropriate view renderer. This is surgical and avoids full re-render of unrelated views.
- **Wave 8E: Timeline steps derive from ai.rec + S fields.** `_getPlanTimeline(ai)` uses `S.bone`, `S.endodonticStatus`, `S.planApproved`, `S.labStatus` to derive clinical step labels, durations, and states (done/active/pending). Never hardcode clinical durations without considering patient risk factors.

- **Phase 4 script deferral pattern (2026-05-16):** Auth/sync scripts (supabase, authModule, serializer, syncQueue, cloudSync, prefsSync, notesEncryption) are loaded with `defer`. The AI/render scripts (calcAI, clinicalEngine, costEngine, all render panels) remain blocking because the inline body script's `init()` IIFE calls them synchronously. The auth/sync init calls were moved from inside the `init()` IIFE to a `DOMContentLoaded` listener outside it. `defer` scripts execute in source order before DOMContentLoaded, so both `denaiAuth` and `denaiSyncQueue` are defined by the time the listener fires.
- **CI auth spec uses TWO sentinels:** (1) `typeof window.render === 'function'` — fires after inline script runs; (2) `window.denaiAuth && getStatus() !== 'reconnecting'` — fires after auth settle. With deferred authModule, both sentinels still work because Playwright `page.goto` waits for `load` event (after DOMContentLoaded), by which time all deferred scripts have run.
- **`text-rendering: optimizeLegibility` removed from body (Phase 4):** This was expensive on Windows/Chrome (forces subpixel font measurement on every text node). Removed for improved TBT. Do NOT re-add.
- **`contain: layout style` on `.sidebar` (Phase 4):** Sidebar layout changes can't escape to page layout; style recalculations inside sidebar are scoped. Safe because sidebar has no `position:fixed` children and its dimensions are fixed (CSS var). Do NOT add `contain:paint` — sidebar has `::after` overlay gradient that should stay.

- **Phase 3 engine scenarios use `s()` override pattern, not full state literals.** All 6 new scenarios merge with BASE via `s({...})`. When asserting rec for RESTORATIVE paths, the `rec` field maps to the *slot* name ('implant'/'bridge'/'crown'), not the treatment ID ('splinted'/'endocrown'/'extract_impl'). Slot mappings: slot1→'implant', slot2→'bridge', slot3→'crown'.
- **Preservation bias threshold is 3 points.** In `recommend()`, `bestPreserve.slot` wins over `extractOpt.slot` only when `bestPreserve.score >= extractOpt.score - 3`. Scenarios must be calibrated so the gap is ≤3 (bias fires) or >3 (extract wins). This is the key lever for COMPROMISED cases.
- **Endocrown ceiling is 94, standard crown ceiling is 96.** Endocrown will never outscore a well-supported standard crown under VIABLE classification. In `endocrown-viable`, the standard crown wins at 96 while endocrown ties crown_core at 94. The test asserts endocrown's slot (crown field) range 88-95, not that it wins.
- **age<18 deferral added to calcAI.js (2026-05-16):** Patients under 18 get `implant -= 15.0; conf -= 15` to model skeletal immaturity. Bridge becomes preferred. Only affects calcAI (single/multi missing tooth paths). The existing `age<40` bonus (+0.6) still applies first; the deferral then overrides it net.

## Key Learnings — UX (Phase 5, 2026-05-17)

- **`href="#"` back-links are broken in SPA:** The case-nav "Back to Cases" used `href="#"` which scrolls to page top instead of navigating. Fix: `href="javascript:void(0)" onclick="navigateTo('cases')"`.
- **Status pill / workflow state must be updated dynamically in `renderMainPanels`:** The `#caseStatusPill` element was hardcoded "In Analysis" in HTML. Add an `id` and update it from `_getWorkflowStage()` in `renderMainPanels`. Map: `scan`→"New Case", `insight`→"In Analysis", `plan`→"Plan Approved", `lab`→"Lab Sent"/"Lab Received".
- **`_getPatientStageBadge` must handle all `labStatus` values explicitly:** `if (p.labStatus)` catches both 'pending' and 'received' under one case. When `labStatus==='received'` the correct label is "Lab Received" not "Lab Sent". Always check received before the generic truthy check.
- **`aria-labelledby` must reference an id that actually exists:** Entry overlay had `aria-labelledby="entryTitle"` but no element with `id="entryTitle"`. Used `entryDesc` (the tagline element) instead. Before writing `aria-labelledby`, verify the target id is in the DOM.
- **"Regenerate" in report history is misleading:** `generateReport()` creates a new report from *current* patient state, not the historical one. Label changed to "Re-run Report" — transparent that it reflects current, not archived, state.
- **Lab "Next Step" CTA when received should navigate to Reports, not AI Insight:** After lab restoration is received, the clinical next action is to generate a delivery report. CTA changed from `navigateTo('insight')` / "AI Insight" to `navigateTo('reports')` / "Go to Reports".
- **Hardcoded fallback strings in dynamic UI are production bugs:** `state.caseNum || 'Case #4587'` shows a fake case number. Always use a neutral placeholder like `'—'` when real data is absent.

## Key Learnings — UX (Phase 5 follow-up, 2026-05-17)

- **`border-top` on a card with `overflow:hidden` clips awkwardly at rounded corners:** CSS borders render in the border-box and are NOT clipped by `overflow:hidden` the same way content is. The border-top's corner curve doesn't align perfectly with the card's clipped `::before` pseudo interior. Fix: replace `border-top: Npx solid <color>` with `box-shadow: <outer-shadow>, inset 0 Npx 0 <color>`. Inset box-shadows render inside the border-radius+overflow boundary, giving clean corner rendering with no clip artifacts.
- **Combining inset + outer box-shadows on `.ai-card`:** `.card` sets `box-shadow: var(--shadow-sm)`. `.ai-card` overrides to `box-shadow: var(--shadow-sm), inset 0 3px 0 var(--c-brand)` — must include the outer shadow explicitly to preserve drop shadow. The base `.card` `transition: box-shadow` still governs animation since `.ai-card` doesn't redefine `transition`.
- **Redundant `border-radius` on `::before` inside `overflow:hidden` card:** `.ai-card::before` had `border-radius: 0 var(--r-xl) 0 0` to match the card's top-right corner. Since the parent card already has `overflow:hidden` + `border-radius`, the pseudo is clipped correctly without its own border-radius. Removed as redundant.
- **Passive encryption indicator pattern — `pointer-events:none; user-select:none`:** A floating/visible lock icon near an editable area reads as interactive (button-like). To make it clearly passive: set `pointer-events:none; user-select:none` on the container span, use `aria-hidden="true"` on the FA icon, add a non-interactive `aria-label` on the container. No `onclick`, no hover style, no cursor:pointer. Updated via JS (`initClinicalNotes`) from `denaiNotesEnc.hasKey()`.
- **Radial gradient `::before` on AI card → notch/triangle artifact (pass 2 correction, 2026-05-17):** The `::before` radial gradient in the top-right corner overlapped visually with the inset box-shadow accent bar, creating a perceptible notch/hump at the junction. Fix: remove `::before` entirely. The inset `box-shadow` accent bar alone is sufficient, clean, and geometry-stable. Simpler is better — decorative gradients over accent bars create seam artifacts.
- **Encryption status: inline text beats chip/badge (pass 2 correction, 2026-05-17):** A chip/badge with a lock icon (even with `pointer-events:none`) reads as interactive because badge shapes carry affordance. Fix: replace with a plain text sibling `<span id="notesEncText">· encrypted</span>` nested inside the status-group span, styled with `color: var(--c-n300)`. Reads as "Auto-saved · encrypted" — purely metadata, no visual affordance. Wrapping status + enc-text in a `display:flex; gap:4px` span keeps them visually grouped without a box/chip shape.

- **Phase 2B Help Layer (2026-05-18): `UIState.helpOpen` is the single help state store.** `helpOpen: {}` added to `UIState`. `toggleHelp(key)` is a top-level function declaration (window-accessible for inline onclick handlers). `.disclosure-body` + `.disclosure-body.open` are the shared CSS primitives reusing the `.why-body.open` animation pattern. Three disclosure points: `conf` (inside `buildAICardStructure` template — interpolates `UIState.helpOpen.conf` at build time), `caseClass` (static HTML in `#caseClassBadge` + sibling `#helpBody_caseClass`), `approvePlan` (dynamic HTML in `renderWorkflowActions` — reads `UIState.helpOpen.approvePlan` at render time). `switchPatient()` resets `UIState.helpOpen = {}` and clears `.disclosure-body.open` DOM nodes. Badge hide branch also clears caseClass help if open.

- **Phase 2B: `buildAICardStructure` is called with `force=true` from `saveEdit()`.** Any template literals inside it that reference `UIState.helpOpen` will re-evaluate at rebuild time. This means the open state is preserved across force-rebuilds without extra DOM patching — the template naturally emits the correct open class and aria-expanded value from current UIState.

- **Phase 2C Bug Reporting (2026-05-18): `_buildDiagnosticPayload(description)` uses explicit allowlist — only safe operational metadata.** Safe fields: appVersion, userAgent, platform, screenSize, currentView, workflowStage, patientCount, authStatus, syncQueueLength, syncQueueStatus, notesEncActive, recentEventTypes (type strings only), userDescription (capped at 500 chars). Explicitly excluded: patient name, tooth, notes, condition, bone, IDs, clinical measurements, reportHistory. `phi_included: false` at JSON root. Returns a plain object — pure function, no side effects.
- **Phase 2C: Report Issue UI lives in BOTH auth modal panels (local + signed-in).** `toggleReportIssue()` syncs both disclosure instances (helpBody_reportIssue_local and helpBody_reportIssue_signedIn) in a forEach loop. Only one panel is ever visible at a time. Uses `UIState.helpOpen['reportIssue']` — participates in `switchPatient()` reset automatically.
- **Phase 2C: `downloadDiagnosticFile(textareaId)` follows the `exportAllData` blob pattern exactly.** Blob → createObjectURL → `<a>` click → CleanupRegistry.timer revoke (500ms). Filename: `denai-diagnostic-<ISO>.json`. `sendDiagnosticEmail(textareaId)` uses `window.location.href = 'mailto:?subject=...&body=...'` — does NOT navigate away, just hands off to the OS mail client. Full JSON payload in body; `encodeURIComponent` handles encoding.
- **Phase 2C: The textarea `id` is passed as a string argument to `downloadDiagnosticFile` and `sendDiagnosticEmail`.** This avoids closures and allows the two panel instances (diagDescLocal, diagDescSignedIn) to share the same functions. The active panel's textarea will have the user's description; the inactive panel's will be empty (no harm — only one is rendered visible at a time).

## Do-Not-Repeat

- [2026-05-15] **`const`/`let` top-level classic-script declarations are NOT `window.*` properties.** In Playwright (and in browsers), `window.X` only resolves for `var` declarations and `function` declarations at script top level. `const ClinicalEngine = ...` makes `ClinicalEngine` available in the global scope but NOT as `window.ClinicalEngine`. Use `typeof X !== 'undefined'` (not `window.X`) when checking for `const`/`let` globals in `page.waitForFunction` or `page.evaluate`. For CI test sentinels, prefer a known function declaration (`window.render`) over `const` globals.



<!-- Mistakes made and corrected. Each entry prevents the same mistake recurring. -->
<!-- Format: [YYYY-MM-DD] Description of what went wrong and what to do instead. -->

- [2026-05-14] **Dual-definition pattern is ONLY safe for `function` declarations, not `const`/`let`.** `function` declarations hoist and silently overwrite; `const`/`let` at top-level of a classic script cannot be re-declared — a second `const X` in the inline script block throws `SyntaxError: Identifier 'X' has already been declared` at parse time, crashing the entire app. For modules containing `const`/`let` globals, collapse Steps B+C into a single atomic operation: insert script tag and remove inline declarations in the same edit session, with no intermediate state.

## Decision Log

<!-- Significant technical decisions with rationale. Why X was chosen over Y. -->

- [2026-05-14] **setState Tier-1 migration (Wave 3.8.1).** Four bypass paths routed through setState(): (1) `selectTx()` multiTooth branch — was `S.tx = option; saveState()`, (2) `applyAIRec()` — was `S.tx = recTx; saveState()`, (3) `whatIfApply` click — was `Object.assign(S, {8 fields}); saveState()`, (4) `_handleNotesInput` — was `S.notes = …; saveState()`. Pattern: remove the explicit `saveState()` call alongside the direct mutation — setState() includes it internally. Intentional exceptions NOT migrated: `switchSite()` (activeSite must not persist), `switchPatient()` / `confirmReset()` (wholesale replacement, not patch), `saveEdit()` BUG#3 correction (post-setState AI check), site2/multiTooth routing handlers (fields absent from StateValidator).

- [2026-05-14] **UIState separation pattern (Wave 3.7.2).** `editing`, `whyOpen`, `historyOpen` moved from `S` to a standalone `UIState` object with a `setUIState(patch)` helper. Three-step sequence: (A) add UIState + setUIState without touching S, (B) migrate all mutation and read sites to UIState (including inline DOM reads within the same toggle functions — must migrate together or DOM breaks), (C) remove stale fields from S init, saveState() destructure, and switchPatient() Object.assign. `activeSite` deliberately left in S — it is a render-routing parameter, not a pure UI flag. `saveState()` destructure was simplified from `const { editing, whyOpen, historyOpen, ...serializable } = S` to `const serializable = { ...S }` since the fields are no longer in S. Persistence boundary is now structural (UIState never reaches saveState()) rather than convention-based (strip list).
